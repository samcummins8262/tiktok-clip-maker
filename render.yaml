services:
  - type: web
    name: tiktok-clip-maker
    env: python
    buildCommand: pip install -r requirements_web.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
